# =============================================================================
# Drone Fuzzy-PSO Optimizer — dependências (versões fixadas p/ reprodutibilidade)
# IA e Computacional (CESUPA) — CC5NA — Prof. Daniel Leal Souza
# Python alvo: 3.11
# -----------------------------------------------------------------------------
# Núcleo científico
numpy==1.26.4
scipy==1.11.4
scikit-fuzzy==0.4.2        # sistema fuzzy Mamdani (skfuzzy.control)
networkx==3.2.1            # dependência do scikit-fuzzy
matplotlib==3.8.4          # superfícies, sensibilidade, convergência
pandas==2.2.2             # tabelas de métricas / resultados
pyyaml==6.0.1             # configs de experimentos

# Back-end (opcional — caminho mínimo viável NÃO depende disto)
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.1
requests==2.32.3          # dashboard -> API (quando USE_API=1)

# Dashboard
streamlit==1.35.0

# Testes
pytest==8.2.0
